# HTML Designer 详细使用指南

HTML Designer 是一个本地优先的可视化 HTML 工作台。它适合编辑独立的 `.html` 或 `.htm` 文档：你可以直接在页面渲染结果上选择元素、修改文字和样式、插入组件，也可以切换到完整源码进行编辑。

## 目录

- [1. 运行要求](#1-运行要求)
- [2. 启动应用](#2-启动应用)
- [3. 首页入口](#3-首页入口)
- [4. 编辑器界面](#4-编辑器界面)
- [5. 选择和编辑元素](#5-选择和编辑元素)
- [6. 组件库](#6-组件库)
- [7. 页面结构树](#7-页面结构树)
- [8. 样式检查器](#8-样式检查器)
- [9. 属性检查器](#9-属性检查器)
- [10. 交互检查器](#10-交互检查器)
- [11. HTML 检查器](#11-html-检查器)
- [12. 源码模式](#12-源码模式)
- [13. 文件保存与导出](#13-文件保存与导出)
- [14. 磁盘对比](#14-磁盘对比)
- [15. 预览](#15-预览)
- [16. 片段](#16-片段)
- [17. AI Design](#17-ai-design)
- [18. 自动保存和撤销](#18-自动保存和撤销)
- [19. 快捷键](#19-快捷键)
- [20. 常见问题](#20-常见问题)
- [21. 安全和隐私](#21-安全和隐私)

## 1. 运行要求

### 基础要求

- Node.js 18 或更高版本。
- 现代桌面浏览器。
- 推荐 Chrome、Edge 或 Arc。

Chrome、Edge、Arc 等 Chromium 浏览器支持 File System Access API，可以把修改直接保存回你选择的文件。

Safari 和 Firefox 可以导入、编辑和下载 HTML，但不能直接覆盖原文件。

### AI Design 要求

AI Design 需要本机已经安装并登录以下任意一种工具：

- Codex CLI
- Claude Code CLI

不使用 AI Design 时，不需要安装这些 CLI。

## 2. 启动应用

### 命令行启动

进入项目目录后执行：

```bash
npm start
```

默认访问地址：

```text
http://localhost:4173/
```

### macOS 一键启动

双击 `start-mac.command`，浏览器会打开：

```text
http://localhost:4175/
```

如果没有执行权限：

```bash
chmod +x start-mac.command
```

### Windows 一键启动

双击 `start-windows.bat`，浏览器会打开 `http://localhost:4175/`。

### 自定义端口

macOS/Linux：

```bash
PORT=5000 npm start
```

Windows PowerShell：

```powershell
$env:PORT=5000
npm start
```

## 3. 首页入口

首页提供四个入口。

### A：打开本地文件

使用系统文件选择器打开 HTML，并建立可写连接。

- 需要 Chromium 浏览器。
- 文件名旁会出现 `linked`。
- 点击“保存”会写回原文件。

### B：导入 HTML

只读取文件内容，不获得写入权限。

- 适合 Safari、Firefox 或不希望覆盖原文件的情况。
- 点击“保存”或“导出”会下载一个文件副本。

### C：新建设计

创建一个响应式示例页面，其中包含 Hero、按钮、卡片和三项功能介绍。可以直接在这个页面上练习编辑。

### D：AI Design

新建示例页面并打开 AI Design 抽屉。

### 拖放导入

把 `.html` 或 `.htm` 文件拖到页面任意位置即可导入。拖放等同于“导入 HTML”，不会建立原文件写入权限。

### 首页快捷键

- `A`：打开本地文件。
- `B`：导入 HTML。
- `C`：新建设计。
- `D`：打开 AI Design。
- `R`：恢复上次草稿；仅在恢复按钮可见时有效。

## 4. 编辑器界面

### 顶部工具栏

顶部包含：

- 画布/源码模式切换。
- 当前文件名和保存状态。
- 撤销、重做、复制、删除、上移、下移和选择父元素。
- 左右侧面板显示/隐藏。
- 桌面、平板和手机画布宽度。
- 沉浸预览和外部预览。
- AI Design。
- 打开、刷新、对比、导入、导出和保存。
- 快捷键速查和深色/浅色主题切换。

### 左侧栏

- 组件：内置组件库和搜索。
- 结构：当前文档的 DOM 结构树。
- 片段：保存并复用你自己的元素。

### 中央区域

画布模式显示 sandbox iframe 中的页面。右上角显示当前设备类型与实际画布尺寸；左下角画布控制器支持缩小、放大、恢复 100%、适应窗口和快速插入组件。源码模式显示完整 HTML 文本编辑器。

在窄屏设备上，组件栏和检查器会自动隐藏以保留完整画布，仍可通过左下角的快速插入按钮添加组件。

### 右侧栏

- 选中摘要：标签、元素说明、CSS 选择器、实时尺寸，以及编辑、复制 HTML 和创建副本。
- 样式：可视化盒模型、字体、布局、间距和表面效果。
- 属性：标签、ID、class 和任意 HTML 属性。
- 交互：生成内联事件处理器。
- HTML：替换当前元素的 outer HTML 或 inner HTML。

### 底部状态栏

左侧显示当前元素从 `body` 开始的层级路径。选择元素后，右侧可以复制元素 HTML 或 CSS 选择器，并显示元素尺寸、状态和最近自动保存时间。

## 5. 选择和编辑元素

### 选择元素

- 单击画布中的元素。
- 在“结构”面板单击节点。
- 点击底部层级路径中的祖先元素。
- 点击工具栏中的父元素按钮。
- 按 `Esc` 取消选择。

选中元素后会出现：

- 蓝色边框。
- 元素标签说明。
- 元素右下方的实时宽高。
- 浮动操作条。
- 宽度、高度和双向尺寸手柄。

浮动操作条提供编辑文字、上移、下移、选择父元素、添加批注、复制和删除。选择列表项或表格单元格时，还会显示对应的添加项目、行或列操作。

### 编辑文字

1. 双击画布文字，或点击浮动工具条中的“编辑文字”。
2. 输入新内容。
3. 点击其他位置或按 Enter 完成。
4. 按 Esc 取消本次编辑。

### 调整尺寸

- 右侧手柄：调整宽度。
- 底部手柄：调整高度。
- 右下角手柄：同时调整宽高。
- 双向调整时按住 Shift：保持原始宽高比例。

尺寸会写入元素的内联 `style`。

### 自由移动

按住浮动工具条左侧的六点拖拽手柄即可自由移动元素。位移会写入元素的 `transform`，移动过程会实时更新选中边框；松开后形成一个可撤销历史步骤。

### 调整顺序

“上移”和“下移”会改变元素在同一父元素中的兄弟节点顺序。

### 复制与删除

- 点击“复制”或按 `Ctrl/⌘ + D`。
- 点击“删除”或按 Delete/Backspace。

## 6. 组件库

当前版本内置 24 个组件，覆盖：

- 文字
- 布局
- 组件
- 媒体
- 列表
- 表单
- 导航
- 数据

### 搜索组件

搜索框会匹配组件分类、名称和 HTML 内容。

### 快速插入

单击组件卡片或点击右侧的 `+` 可立即插入该组件。组件卡片支持键盘焦点，按 Enter 或空格也能插入。点击“快速插入”、画布左下角的组件按钮，或按 `I`，会打开可搜索的组件面板；使用 `↑`、`↓` 选择，按 Enter 插入，按 Esc 关闭。

插入位置会根据当前选择的 HTML 语义判断：布局和卡片等块级内容会放在标题、段落等文本元素附近；徽标等行内内容可以放入合适的文本容器。没有选择元素时，组件会插入 `body`。

### 拖入组件

1. 从左侧按住组件。
2. 拖到画布元素附近。
3. 粉色标记显示插入位置。
4. 松开鼠标完成插入。

落点位于目标顶部时插入到目标之前，位于底部时插入到目标之后，位于中部时优先插入目标内部。

拖动由 HTML Designer 的跨画布交互处理，鼠标进入画布后仍会保留组件和落点信息。

## 7. 页面结构树

“结构”面板显示 `body` 及其可编辑子元素。

- 单击节点选择对应元素。
- 当前元素会高亮。
- 点击箭头折叠或展开子树。
- 节点文字使用标签、ID 和 class 描述元素。
- 结构节点可以拖到画布中重新定位。
- 结构节点之间也可以直接拖拽；节点上方、内部和下方会显示不同落点反馈。

`script` 和 `style` 不会作为普通画布结构节点显示。需要编辑它们时请使用源码模式。

## 8. 样式检查器

样式修改会写入当前元素的内联 `style`。

检查器顶部先显示元素摘要，便于确认当前标签、选择器和外框尺寸。摘要下方的盒模型按 margin、border、padding 和 content 分层展示四边数值；content 中显示扣除边框与内边距后的内容区尺寸。

### 字体

- 字体
- 字号
- 字重
- 行高
- 文本对齐
- 文字颜色

### 布局

- display
- position
- width
- height
- max-width
- gap
- flex-direction
- justify-content
- align-items

### 间距

- margin
- padding

可以输入任意有效 CSS 值，例如：

```text
24px
1.5rem
clamp(20px, 4vw, 64px)
0 auto
```

### 外观

- 背景色
- 圆角
- 边框
- 阴影
- 透明度
- transform

## 9. 属性检查器

### 更换标签

输入新的合法标签名，例如把 `div` 改为 `section`。子节点和已有属性会保留。

### ID

设置或清空元素的 `id`。

### Classes

在“添加”中输入一个或多个 class，使用空格分隔。点击 class 标签上的 × 可以删除。

### 任意属性

可以修改已有属性的名称和值，也可以添加：

```text
aria-label
data-state
role
href
src
alt
name
```

无效属性名不会被应用。

## 10. 交互检查器

支持的事件：

- onclick
- ondblclick
- onmouseenter
- onmouseleave
- oninput
- onchange
- onsubmit
- onfocus
- onblur

支持的动作：

- `link`：跳转 URL。
- `alert`：显示浏览器提示。
- `toggle`：切换目标元素的 hidden 状态。
- `toggle-class`：切换目标元素 class。
- `custom`：写入自定义 JavaScript。

切换 class 的参数格式：

```text
.menu open
```

前半部分是 CSS 选择器，最后一项是 class 名称。

交互会写入 `onclick` 等内联属性。编辑画布不执行脚本，请使用外部预览测试。

## 11. HTML 检查器

### Outer HTML

替换当前元素本身和全部子内容。必须包含一个顶层元素：

```html
<article class="card">
  <h2>New card</h2>
  <p>Card content</p>
</article>
```

### Inner HTML

只替换当前元素内部内容，可以包含多个并列节点：

```html
<h2>Title</h2>
<p>Description</p>
```

应用 HTML 后，结构树会重新渲染。

## 12. 源码模式

点击顶部“源码”进入完整 HTML 编辑器。

源码栏会实时显示文档行数和字符数，长行使用水平滚动，避免换行改变阅读位置。

适合：

- 编辑 `<head>`。
- 修改内联 CSS。
- 添加或检查脚本。
- 进行大范围结构调整。
- 精确控制源文本。

切回“画布”后，源码会重新载入 iframe。浏览器可能自动修正不完整或不合法的 HTML。

如果没有修改源码，往返切换不会额外产生脏状态；发生修改后会增加一个“源码编辑”历史步骤。

## 13. 文件保存与导出

### 关联文件

通过“打开本地文件”载入后：

- 文件名旁显示 `linked`。
- 保存按钮直接写回原文件。
- 保存前会检查文件是否在磁盘中被其他程序更新。

如果磁盘版本较新，应用会要求确认后才覆盖。

### 导入或新建文档

没有文件写入句柄时，“保存”和“导出”都会下载 HTML 文件。

### 刷新

关联文件可以使用“刷新”重新读取磁盘内容。存在未保存修改时会先要求确认。

## 14. 磁盘对比

关联文件可以点击“对比”，并排查看：

- 左侧：当前磁盘文件。
- 右侧：编辑器将要保存的内容。

可以复制编辑器完整内容。当前对比视图强调安全核对，不会自动合并差异。

## 15. 预览

### 沉浸预览

点击圆形预览按钮或按 `P`，隐藏编辑器界面，只显示页面。

- 按 `P` 再次切换。
- 按 Esc 退出。
- 点击右下角“退出预览”。

沉浸预览仍位于安全 iframe 中，不执行脚本。

### 外部预览

点击外链按钮，在新的临时标签页打开当前 HTML。外部预览会执行页面脚本。

外部预览使用临时 Blob URL，相对图片、CSS 或脚本路径可能无法解析。依赖项目目录结构时，应先保存文件，再通过真实项目服务器访问。

### 设备宽度

桌面、平板和手机按钮只改变画布宽度，用于检查响应式 CSS；不会模拟真实设备 User-Agent、触摸输入或网络条件。

### 画布缩放

左下角控制器支持 50%–150% 缩放。点击百分比恢复 100%，点击四角图标根据当前设备宽度适应窗口。缩放只影响编辑视图，不会写入导出的 HTML。

## 16. 片段

### 保存片段

1. 选择一个元素。
2. 切换到“片段”。
3. 点击“保存当前元素为片段”。
4. 输入名称。

### 使用片段

- 拖动片段到画布。
- 双击片段快速插入。
- 点击 × 删除片段。

片段保存在当前浏览器 localStorage 中，最多保留 50 条。

## 17. AI Design

AI Design 通过本地 Node 服务调用 Codex CLI 或 Claude Code CLI。

### 配置

1. 点击“AI Design”。
   打开后会暂时收起组件栏和检查器，让画布与 AI 面板并排显示；关闭后恢复原有布局。也可以按 Esc 关闭。
2. 展开“连接设置”。
3. Endpoint 通常保持 `/api/ai-design`。
4. 选择 Codex CLI 或 Claude Code CLI，模型列表会自动从当前 CLI 刷新。
5. 从模型下拉框中选择一个可用模型，或保留“使用 CLI 默认模型”。Codex 会读取完整的 CLI 模型目录；Claude Code 会读取 CLI 公布的别名和本机配置，也可以选择“手动输入模型 ID”。
6. 建议保持“自动切换可用 CLI”开启。当前 CLI 无法启动、未授权、超时或模型网关不兼容时，任务会继续尝试本机另一条 CLI 链路。
7. 保存设置。
8. 点击“测试连接”。测试不再只读取版本号，而会发送一次最小真实请求，同时检查 CLI 启动、登录、模型服务和输出链路。

连接状态会显示在设置顶部。发生自动切换后，成功完成任务的 CLI 会保存为后续默认选项；你仍可随时手动切回并重新测试。

### 修改整个页面

不选择元素时输入：

```text
保持现有文案，把页面改成简洁的企业产品官网，并优化手机端布局。
```

AI 会收到当前完整 HTML。

### 修改选中元素

选择元素后打开 AI Design，顶部会显示目标。此时 AI 还会收到：

- 元素描述。
- DOM 路径。
- 元素 outer HTML。

提示示例：

```text
只重新设计当前卡片，增加状态徽标和一个次要按钮，其他页面内容保持不变。
```

### 组件批注

1. 选择元素。
2. 点击浮动工具条中的“批注”。
3. 输入修改意见并添加。
4. 对多个组件重复操作。
5. 在批注列表勾选需要发送的意见。
6. 点击“发送选中批注”。

### 停止任务

运行期间点击“停止”会中断浏览器请求。本地服务收到连接关闭后会终止对应 CLI 子进程。

### 环境变量

```bash
HTML_DESIGNER_AGENT_CLI=codex
HTML_DESIGNER_AGENT_MODEL=
HTML_DESIGNER_AGENT_TIMEOUT_MS=180000
HTML_DESIGNER_AGENT_PROBE_TIMEOUT_MS=60000
HTML_DESIGNER_AGENT_OUTPUT_LIMIT=8000000
HTML_DESIGNER_MODEL_CATALOG_TTL_MS=300000
```

## 18. 自动保存和撤销

### 自动保存

修改后约 1.5 秒，应用把以下内容保存到 localStorage：

- 当前 HTML。
- 当前文件名。
- 保存时间。

自动保存用于恢复浏览器会话，不会写入磁盘文件。

### 撤销历史

Visual 模式最多保留 36 个文档快照。以下操作会建立历史步骤：

- 修改样式。
- 编辑文字。
- 插入、复制、删除和移动元素。
- 调整尺寸。
- 修改标签、class 和属性。
- 设置交互。
- 修改 outer/inner HTML。
- 从 Source 返回且源码发生变化。

源码文本框内部编辑可以使用浏览器原生撤销；切回画布后会形成一个可视化历史步骤。

## 19. 快捷键

macOS 使用 `⌘`，Windows/Linux 使用 `Ctrl`。

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl/⌘ + S` | 保存或下载导出 |
| `Ctrl/⌘ + O` | 打开本地文件 |
| `Ctrl/⌘ + K` | 打开命令面板 |
| `Ctrl/⌘ + Z` | Visual 模式撤销 |
| `Ctrl/⌘ + Shift + Z` | Visual 模式重做 |
| `Ctrl/⌘ + D` | 复制当前元素 |
| `Delete` / `Backspace` | 删除当前元素 |
| `Shift + ↑` | 选择父元素 |
| `Alt/Option + ↑` | 当前元素上移 |
| `Alt/Option + ↓` | 当前元素下移 |
| `Esc` | 取消选择或退出预览 |
| `P` | 切换沉浸预览 |
| `?` | 打开快捷键速查 |
| `/` | 聚焦组件搜索 |
| `I` | 打开快速插入组件面板 |
| 双击文字 | 就地编辑 |
| 调整尺寸时按 Shift | 保持宽高比例 |

在 input、textarea、select 或可编辑文字中，除保存外的全局快捷键会让位给文本编辑。

## 20. 常见问题

### npm start 后打不开 4175

`npm start` 默认端口是 4173；双击启动脚本默认端口是 4175。以终端输出为准。

### “打开本地文件”变成导入

当前浏览器或访问环境不支持 File System Access API。请使用 Chrome/Edge，并通过 localhost 或 HTTPS 访问。

### 保存按钮下载了文件

当前文档没有可写文件句柄。使用“打开本地文件”重新载入，才能直接覆盖原文件。

### JavaScript 在画布中不运行

这是安全设计。请使用外部预览。

### 图片或样式丢失

相对路径会相对于 HTML Designer 服务地址或 Blob URL 解析，而不是原文件目录。可以：

- 使用绝对 URL。
- 设置 `<base href="...">`。
- 保存后通过原项目开发服务器预览。

### AI Design 连接失败

检查：

1. 是否使用 `server.js` 启动，而不是普通静态服务器。
2. CLI 是否安装。
3. CLI 是否完成登录。
4. Endpoint 是否正确。
5. 模型名是否有效；不确定时留空。
6. 是否超过默认 180 秒超时。
7. 点击“测试连接”查看每条 CLI 的真实请求结果。

如果提示“本机 API 网关不接受 Claude Code 的系统提示格式”，表示 `ANTHROPIC_BASE_URL` 对应服务没有正确实现 Anthropic Messages 协议：`system` 应作为顶层字段处理，`messages` 中只能出现 `user` 和 `assistant`。可以修正该网关，或开启“自动切换可用 CLI”让任务继续使用 Codex CLI。

### 恢复按钮没有出现

- 尚未产生修改。
- 关闭页面前 1.5 秒自动保存尚未完成。
- localStorage 被清除或禁用。
- 当前使用不同域名、端口或浏览器配置文件。

## 21. 安全和隐私

### 文件处理

普通打开、导入、可视化编辑、源码编辑和自动保存都发生在浏览器中。应用只能访问你明确选择的文件。

### iframe 沙箱

编辑 iframe 允许同源访问和表单控件，但不允许脚本执行。页面提交会被阻止，链接导航也会被编辑器接管。

### 外部预览

外部预览会执行页面脚本。只预览你信任的 HTML。

### AI Design

AI Design 会把当前 HTML、选中元素和文字要求发送给本机 CLI。CLI 可能根据自身配置连接模型服务。

不要向 AI Design 提交不允许发送给模型服务的密钥、个人信息或机密源码。

### 自定义交互

“custom” 动作会把 JavaScript 直接写入最终 HTML。发布前应人工检查。

---

- 项目主页：`https://github.com/lamp-cat/html-designer`
- GitHub Pages：`https://lamp-cat.github.io/html-designer/`
